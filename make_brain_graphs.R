# The idea behind this script is to make something like
# wholebrain::plot.outlines(regi, TRUE) but in ggplot2

# given a regi file

library(tidyverse)       





write.csv(df, str_replace(filename, pattern = ".tif", ".csv" ), row.names = FALSE)

